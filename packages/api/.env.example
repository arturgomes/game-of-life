# Server Configuration
PORT=
NODE_ENV=

# WebSocket Configuration
# Override WebSocket URL for development (frontend connects from different origin)
WS_HOST=
WS_PROTOCOL=

# MongoDB Configuration (use docker-compose service name in container)
MONGODB_URI=

# Redis Configuration (use docker-compose service name in container)
REDIS_URL=

# Cache Configuration (in seconds)
CACHE_TTL_CURRENT=
CACHE_TTL_GENERATION=
CACHE_TTL_FINAL=

# WebSocket Configuration
WS_HOST=
WS_PROTOCOL=